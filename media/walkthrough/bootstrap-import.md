## Bootstrap or Import

**New projects** — Run `/bootstrap` in chat or use the Command Palette. This creates the `project_memory/` folder structure, optionally initialises Git, and scaffolds CI/governance files.

**Existing projects** — Run `/import` to scan your codebase. AtlasMind detects dependencies, conventions, and project type, then populates memory entries automatically.
