## Send Your First Message

Type `@atlas` followed by your question in the VS Code chat panel:

```
@atlas explain the architecture of this project
```

AtlasMind selects the best agent and model based on your budget and speed settings, retrieves relevant project memory, and streams a response.

Explore slash commands: `/agents`, `/skills`, `/memory`, `/cost`.
